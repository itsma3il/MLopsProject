name: fraud_detection
version: "1.0.0"
config-version: 2

profile: fraud_detection

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  fraud_detection:
    staging:
      +materialized: view
    marts:
      +materialized: table
